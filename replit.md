# Replit Static Web Project

## Overview
A basic static web application with HTML, CSS, and JavaScript.

## Structure
- `index.html` - Main HTML entry point
- `style.css` - Stylesheet
- `script.js` - JavaScript logic

## Running
The project is served via `static-web-server` on port 80. The workflow "Serve static" handles this automatically.
